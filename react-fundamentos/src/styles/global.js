// Aqui ficam os estilos globais

import { createGlobalStyle } from 'styled-components';

// `` - usado pelo style components
export default createGlobalStyle`
  body {
    background: #222;
    font-family: sans-serif;
    color: #fff;
  }
`;
